// Omitir atributos ou colocar como não obrigatórios
//import { OmitType, PartialType } from '@nestjs/swagger';
import { TodoEntity } from '../entity/todo.entity';

export class IndexTodoSwagger extends TodoEntity {}
